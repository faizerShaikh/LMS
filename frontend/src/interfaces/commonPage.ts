export interface CommonPageInterface{
    id: string;
      name: string;
      coverImage: string;
      title: string;
      titleDescription: string;
      pageDescription: string;
      createdAt: string;
      updatedAt: string;
}